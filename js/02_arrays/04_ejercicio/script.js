console.log("conectando correctamente...");

/*
Enunciado:
Crea una función que multiplique los elementos
ubicados en posiciones pares (0, 2, 4) de un 
arreglo. Muestra el resultado con un alert.

*/

function multiplicaPares() {
    let numeros= [10,1,2,3,4,5];
    let multiplica= ( numeros[4]);
    alert("la multiplicacion de sus pociciones sera el resultado: \n"
    +multiplica);
}
